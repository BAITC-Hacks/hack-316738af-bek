# Empty = same origin /api. For a separate API, use its origin (without /api).
# Vite development proxy forwards /api to http://127.0.0.1:8000.
VITE_API_BASE_URL=
# No API keys belong in VITE_* variables. Demo is explicitly opened with ?demo=1.
