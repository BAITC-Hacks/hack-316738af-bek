OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1
NVIDIA_API_KEY=
NVIDIA_EMBED_MODEL=nvidia/llama-nemotron-embed-1b-v2
NVIDIA_ENABLED=false
ANALYSIS_TIMEOUT_SECONDS=600
DATA_DIR=backend/data
APP_ACCESS_TOKEN=
# For a public demo, set a separate random access code (not an API key).
# Judges enter this code at /access. Existing API Bearer authentication also works.
COOKIE_SECURE=false
# Separate frontend development only, e.g. http://localhost:5173
CORS_ORIGINS=
